image: gcc

stages: 
  - build

init:
  stage: .pre
  script:
    - echo "This is the initialization of the CI/CD"
    - apt update && apt -y install make autoconf


cmake:
  stage: build
  script:
    - mkdir build && cd build
    - cmake ..
    - make
